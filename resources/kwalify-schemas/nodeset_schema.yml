type: map
mapping:
  default_set: { type: str }
  sets:
    type: map
    mapping:
      =:
        type: map
        mapping:
          default_node: { type: str }
          nodes:
            type: map
            mapping:
              =:
                type: map
                mapping:
                  prefab: { type: str }
                  options:
                    type: map
                    mapping:
                      ip: { type: str }
                      memory: { type: int }
                      cpus: { type: int }
                  role: { type: str }
