type: map
mapping:
  default_set: { type: str }
  sets:
    type: map
    mapping:
      =:
        type: map
        mapping:
          default_node: { type: str }
          nodes:
            type: map
            mapping:
              =:
                type: map
                mapping:
                  prefab: { type: str }
                  role: { type: str }
